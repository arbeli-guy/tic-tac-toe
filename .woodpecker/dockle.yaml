---
  when:
    event: push
    branch: feature/dockle
#
  steps:
    dockle:
      image: euryecetelecom/woodpeckerci-dockle
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock
      settings:
        dockerfile: client/Dockerfile.prod